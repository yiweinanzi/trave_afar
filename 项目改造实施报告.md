# GoAfar 项目改造实施报告

更新时间：2026-02-14

## 1. 项目现状梳理（改造前）

### 1.1 原始链路
1. 入口分叉：`main.py` / `run_with_llm.py` / `app.py` 各自维护流程。
2. 召回：`vector_builder.py` dense 检索 + `candidate_merger.py` 流行度近似。
3. 重排：模板规则/可选 LLM。
4. 规划：`time_matrix_builder.py` Haversine 时间矩阵 + `vrptw_solver.py`。
5. 文案：模板优先 + 可选 LLM。

### 1.2 关键问题
1. `poi_id` 跨文件格式不一致（`poi.csv` 如 `001001`，`user_events.csv` 如 `1001`），导致行为召回失真。
2. 硬编码路径较多（模型目录绝对路径）。
3. 向量产物缺失时流程易失败，缺少统一自愈。
4. Web/CLI 业务链路不一致，难以保证行为一致性。

## 2. 本次改造（对齐 `gaozao.md`）

### 2.1 P0 工程收敛
1. 新增统一配置：`configs/runtime.yaml`。
2. 新增配置加载：`src/service/config.py`。
3. 新增统一契约（Pydantic）：`src/schemas/recommendation.py`。
4. 新增统一编排层：`src/service/pipeline.py`。
5. 三入口统一接入 pipeline：
   - `main.py`
   - `run_with_llm.py`
   - `app.py`

### 2.2 召回体系升级
1. `src/recommendation/candidate_merger.py` 重构为 Multi-Recall：
   - semantic recall
   - behavior recall（基于行为权重 + 用户近期行为）
   - geo recall
2. 引入 RRF 融合与分路分数校准。
3. 修复 `poi_id` 不一致问题：
   - 新增 `src/utils/id_mapping.py`
   - 在召回/训练/向量元数据中统一 ID 归一化

### 2.3 检索与自愈
1. `src/embedding/vector_builder.py` 增加：
   - `ensure_embedding_artifacts` 启动自愈
   - FAISS 可选加速（自动降级 numpy）
   - 模型路径环境变量支持（去硬编码）
2. `src/embedding/build_embeddings_gpu.py` 同步去硬编码并统一 ID。

### 2.4 规划层升级
1. `src/routing/time_matrix_builder.py` 重构为 provider 模式：
   - `OSRMTimeMatrixProvider`
   - `HaversineTimeMatrixProvider`
   - `auto` 自动回退
2. 增加时间矩阵缓存接入（`CacheManager`）。

### 2.5 服务化能力
1. 新增 FastAPI 服务入口：`src/api/server.py`
   - `/healthz`
   - `/readyz`
   - `/v1/recommend/itinerary`

### 2.6 数据飞轮基础
1. 新增训练数据合成脚本：`src/data_processing/synthesize_training_data.py`
   - `planner_trajectories.jsonl`
   - `planner_preference_pairs.jsonl`
   - `planner_rl_prompts.jsonl`
2. 新增外部数据下载清单：`data/external/README.md`

### 2.7 RL/GRPO 训练骨架
1. 新增 GRPO 配置模板：`configs/grpo_planner.yaml`
2. 新增 RL 奖励管理器：`src/rl/reward_manager.py`
3. 新增 RL/SFT 数据构建器：`src/rl/dataset_builder.py`

## 3. 当前仍建议继续推进（Phase 2+）

1. 接入真实 RecBole 在线推理 provider（当前 behavior recall 已增强，但仍非完整在线 RecBole serving）。
2. 增加多目标精排训练与在线推理模块（当前以召回+重排为主）。
3. 增加 RL/GRPO 训练工程（veRL）与 Hybrid repair（RL 初解 + OR-Tools 修复）完整闭环。
4. 补充更系统的 `pytest` 集成测试（目前新增了 schema contract 示例）。

## 4. 你需要下载的数据（手动）

见：`data/external/README.md`

优先级建议：
1. Gowalla（行为序列）
2. GeoLife（轨迹）
3. Solomon VRPTW（规划 benchmark）
4. Yelp Open Dataset（POI 文本与偏好补充）

## 5. 本次改造涉及的关键文件

1. `configs/runtime.yaml`
2. `src/service/config.py`
3. `src/service/pipeline.py`
4. `src/schemas/recommendation.py`
5. `src/recommendation/candidate_merger.py`
6. `src/embedding/vector_builder.py`
7. `src/routing/time_matrix_builder.py`
8. `src/api/server.py`
9. `src/data_processing/synthesize_training_data.py`
10. `main.py`
11. `run_with_llm.py`
12. `app.py`
