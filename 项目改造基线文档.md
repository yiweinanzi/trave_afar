# GoAfar 项目改造基线文档

更新时间：2026-02-14

## 1. 文档目的

这份文档用于后续代码改造的基线对齐，重点回答三件事：

1. 当前项目“实际在跑”的链路是什么（而不是宣传口径）。
2. 每个模块的输入/输出契约是什么，哪些是硬前置。
3. 下一步改造应优先做什么，风险点在哪里。

---

## 2. 项目概览（基于代码实查）

GoAfar 是一个旅游路线推荐系统，主流程由以下模块组成：

1. 语义召回：BGE-M3 向量检索（`src/embedding`）
2. 候选融合：语义结果 + 流行度结果融合（`src/recommendation/candidate_merger.py`）
3. 意图理解与重排序：模板规则为主，可切换 LLM（`src/llm4rec`）
4. 路线规划：OR-Tools VRPTW（`src/routing`）
5. 文案生成：模板优先，LLM 可选（`src/content_generation`）

主要入口：

1. `main.py`：标准流程（默认模板模式）
2. `run_with_llm.py`：Qwen 增强流程
3. `app.py`：Gradio Web UI

---

## 3. 代码目录与职责

`src/` 下核心模块：

1. `src/data_processing/`
   - `sql_extractor.py`：从 SQL 提取 POI，生成 `data/poi.csv`
   - `event_generator.py`：生成模拟 `user_events.csv`
2. `src/embedding/`
   - `bge_m3_encoder.py`：BGE-M3 编码封装
   - `vector_builder.py`：构建向量、语义检索
   - `build_embeddings_gpu.py`：GPU 版向量构建（带缓存）
3. `src/recommendation/`
   - `candidate_merger.py`：语义召回 + 流行度召回融合
   - `recbole_trainer.py`：RecBole 数据导出与训练脚本
4. `src/llm4rec/`
   - `intent_understanding.py`：意图理解（模板/LLM）
   - `llm_reranker.py`：候选重排（规则/LLM）
   - `qwen_recommender.py`：Qwen 统一推荐器（意图/重排/文案）
   - `item_encoding.py`、`explanation.py`：增强与解释模块
5. `src/routing/`
   - `time_matrix_builder.py`：Haversine 时间矩阵
   - `vrptw_solver.py`：VRPTW 求解
   - `multi_day_planner.py`：多日规划（扩展模块）
6. `src/content_generation/`
   - `title_generator.py`：模板文案
   - `llm_generator.py`：本地/接口 LLM 文案
   - `train_dpo.py`、`make_prefs.py`：DPO 偏好训练链路
7. `src/evaluation/`
   - `metrics.py`：Recall/NDCG/路线质量评测
   - `resume_generator.py`：简历文案产出（辅助）
8. `src/utils/`
   - `model_downloader.py`：模型下载
   - `cache_manager.py`：通用缓存（当前主流程未深度接入）

---

## 4. 实际运行链路（按入口）

### 4.1 `main.py`（标准流程）

执行顺序：

1. `IntentUnderstandingModule(use_template=True)` 做意图解析
2. `merge_candidates()` 召回与融合
3. `LLMReranker(use_template=True)` 重排
4. `build_time_matrix()` 构建候选时间矩阵
5. `VRPTWSolver.solve()` 求解单日路线
6. `GoAfarLLM(mode='template')` 生成标题和描述

结论：

1. 标准流程默认是“模板主导”，不是强依赖在线大模型。
2. 具备端到端闭环，适合作为重构基线。

### 4.2 `run_with_llm.py`（Qwen 增强流程）

执行顺序：

1. 初始化 `QwenRecommender`
2. `understand_intent()` 意图理解
3. `merge_candidates()` 候选召回
4. `rerank_pois()` LLM 重排（候选过多或模型不可用会回退）
5. VRPTW 路线规划
6. `generate_content()` LLM 文案生成（失败回退模板）

结论：

1. 具备 LLM 增强能力，但大量分支内置降级策略。
2. 模型路径写死较多，迁移成本偏高。

### 4.3 `app.py`（Web UI）

执行顺序（与 `main.py` 不完全一致）：

1. 意图理解（模板）
2. 直接 `search_similar_pois(topk=100)` 做语义召回
3. 可选重排
4. VRPTW 求解
5. 模板文案生成

关键差异：

1. Web UI 未调用 `merge_candidates()`，即不走“语义+流行度融合”。
2. Web UI 与 CLI 存在流程分叉，后续建议统一服务层。

---

## 5. 数据与产物契约

### 5.1 输入数据

1. `data/poi.csv`
   - 字段：`poi_id,name,lat,lon,open_min,close_min,stay_min,province,city,description,time_str,airport`
   - 当前行数：1333
2. `data/user_events.csv`
   - 字段：`user_id,poi_id,timestamp,action`
   - 当前行数：38579
   - 行为分布：`click`/`fav`/`visit`

### 5.2 运行中间产物

1. `outputs/emb/poi_emb.npy`：POI dense 向量
2. `outputs/emb/poi_meta.csv`：向量对应元数据
3. `outputs/routing/time_matrix.npy`：时间矩阵（可覆盖）
4. `outputs/results/*.json`：推荐结果
5. `outputs/recbole/custom/goafar.inter`：RecBole 训练输入

### 5.3 硬前置条件

1. 语义检索依赖 `outputs/emb/poi_emb.npy` + `outputs/emb/poi_meta.csv`
2. 如果缺失向量文件，Web UI 和召回流程会直接失败
3. 多个模块依赖本地模型目录（绝对路径）

---

## 6. 真实接入状态（重要）

### 6.1 已在主流程真实接入

1. BGE-M3 dense 向量检索
2. 意图理解模板模式
3. 规则重排
4. VRPTW 单日规划
5. 模板文案生成
6. Qwen 增强流程（可选，带降级）

### 6.2 存在但未在主流程深度接入

1. RecBole 推理结果未直接进入在线召回
   - 当前 `candidate_merger.py` 的“序列召回”是流行度近似（`_get_popular_pois`）
2. `cache_manager.py` 未系统接入主流程
3. `multi_day_planner.py` 未接入入口脚本
4. DPO 训练链路存在，但未与线上文案生成策略自动打通

---

## 7. 关键技术债与风险点

### P0（优先处理）

1. 绝对路径硬编码过多
   - 例如 `/root/autodl-tmp/goafar_project/models/...`
   - 影响部署迁移、CI、容器化
2. 流程分叉
   - `main.py`、`run_with_llm.py`、`app.py` 各自维护流程逻辑
   - 容易出现行为不一致和修复遗漏
3. 运行前置强依赖离线产物
   - 向量文件不存在时系统不可用
   - 缺少统一初始化/自愈机制

### P1（建议尽快处理）

1. RecBole 训练与在线服务解耦不足
   - 训练脚本存在，但在线侧仍以流行度近似代替
2. 参数与配置分散
   - 速度、batch、topk、模型路径等分布在多个脚本
3. 数据类型一致性风险
   - `user_events.csv` 中 `poi_id` 与 `poi.csv` 的 ID 体系可能发生类型/格式不一致

### P2（中期优化）

1. 路网时间使用 Haversine + 固定速度
   - 与真实道路时间存在偏差
2. 日志体系以 `print` 为主
   - 缺少统一日志等级、追踪 ID、可观测性
3. 测试体系偏脚本化
   - 缺少 `pytest` 风格单测和契约测试

---

## 8. 建议改造路线（可直接执行）

### 阶段 A：先做工程收敛（1-3 天）

1. 提取统一配置层
   - 新建 `configs/runtime.yaml` 或 `.env`，统一模型路径/数据路径/topk/超参
2. 建立统一服务编排层
   - 把 `main.py`、`run_with_llm.py`、`app.py` 共用逻辑抽到 `src/service/pipeline.py`
3. 引入路径管理
   - 全量替换硬编码路径，使用 `Path(__file__)` + 项目根目录解析

### 阶段 B：再做算法链路一致化（3-7 天）

1. 定义标准 `CandidateProvider` 接口
   - `SemanticRecallProvider`
   - `PopularityProvider`
   - `RecBoleProvider`（真正接入训练结果）
2. 统一重排接口
   - `Reranker` 支持 `rule` / `llm` 策略切换
3. 统一返回 schema
   - 确保 CLI、Web、脚本返回字段一致

### 阶段 C：可用性与质量（3-7 天）

1. 向量文件缺失自动构建或友好降级
2. 增加核心单测
   - 意图提取、候选融合、路由返回格式、文案生成
3. 增加集成测试
   - 标准流程、LLM流程、Web流程共用同一 pipeline 测试

### 阶段 D：效果提升（可并行）

1. 接入真实路网时间（地图 API 或 OSM 路网）
2. 引入多日规划至主流程
3. 把 DPO 微调模型纳入文案生成选择器

---

## 9. 建议先改的文件清单

第一批（高收益）：

1. `main.py`
2. `run_with_llm.py`
3. `app.py`
4. `src/recommendation/candidate_merger.py`
5. `src/embedding/vector_builder.py`
6. `src/llm4rec/qwen_recommender.py`
7. `src/content_generation/llm_generator.py`

第二批（质量与可维护性）：

1. `src/routing/time_matrix_builder.py`
2. `src/routing/multi_day_planner.py`
3. `src/utils/cache_manager.py`
4. `test_pipeline.py`
5. `test_full_pipeline.py`

---

## 10. 当前可执行的最小工作流

1. 准备数据（若缺失）
   - `python -c "from src.data_processing.sql_extractor import parse_go_address_sql; parse_go_address_sql()"`
2. 生成向量
   - `python src/embedding/build_embeddings_gpu.py`
3. 跑标准链路
   - `python main.py`
4. 跑 Web UI
   - `python app.py --port 7860`

---

## 11. 本次梳理说明

本文件依据源码与脚本实查生成，重点参考：

1. `main.py`
2. `run_with_llm.py`
3. `app.py`
4. `src/` 下各核心模块
5. `configs/recbole.yaml`
6. `test_pipeline.py` 与 `test_full_pipeline.py`

已执行的轻量校验：

1. 语法编译检查（`compileall`）通过

未执行项（本环境限制）：

1. 端到端运行与模型推理实测（当前执行环境缺少运行依赖，如 `pandas`）

---

## 12. 你接下来可以直接基于本文档做的改造

1. 先统一配置与路径，消除硬编码，保证可移植性。
2. 再统一三入口到一个 pipeline service，避免逻辑分叉。
3. 最后把 RecBole 在线推理接进候选融合，替换流行度近似。

